rules_version = '2';
match /b/{bucket}/o {
  // Anyone can read files
  match /{allPaths=**} {
    allow read: if true;
  }
  
  // Only authenticated users can upload files
  match /{path}/{allPaths=**} {
    allow write: if request.auth != null;
  }
}
