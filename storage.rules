rules_version = '2';
match /b/{bucket}/o {
  // Anyone can read images
  match /{allPaths=**} {
    allow read: if true;
    // Only authenticated users can upload
    allow write: if request.auth != null;
  }
}
